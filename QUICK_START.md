# 🚀 Быстрая шпаргалка: Деплой на Amvera

## ✅ Чек-лист перед деплоем

- [ ] Создан файл `.env` с реальными токенами
- [ ] Все изменения закоммичены в Git
- [ ] Репозиторий запушен на GitHub
- [ ] Получен токен бота у @BotFather

## 📦 Необходимые файлы (уже созданы)

- ✅ `docker-compose.yml` - оркестрация сервисов
- ✅ `src/Dockerfile` - образ для backend
- ✅ `bot/Dockerfile` - образ для бота
- ✅ `.dockerignore` - исключения для Docker
- ✅ `.env.example` - шаблон переменных
- ✅ `amvera.yml` - конфигурация Amvera

## 🔧 Шаги для деплоя

### 1. Создайте `.env` файл:
```bash
cp .env.example .env
# Отредактируйте .env и заполните реальные значения
```

### 2. Запушьте код на GitHub:
```bash
git add .
git commit -m "Prepare for Amvera deploy"
git push origin main
```

### 3. На сайте Amvera (https://amvera.ru):
1. Создать проект
2. Подключить GitHub репозиторий
3. Выбрать тип: **Docker Compose**
4. Порт: **5000**
5. Добавить переменные окружения:
   ```
   BOT_TOKEN=...
   ADMIN_PASSWORD=...
   MINIO_SECRET_KEY=...
   ```
6. Нажать **Deploy**

### 4. После деплоя - обновите URL:
```powershell
# Откройте update_urls.ps1
# Замените YOUR_PROJECT_NAME на ваше имя проекта
# Запустите: .\update_urls.ps1
```

Или вручную замените в файлах `docs/app.js` и `docs/admin.html`:
```
https://thefid.pythonanywhere.com → https://your-project.amvera.io
```

### 5. Настройте Telegram Mini App:
1. Откройте @BotFather
2. `/setmenubutton` → выберите бота
3. Текст кнопки: **"Открыть приложение"**
4. URL: `https://your-project.amvera.io/docs/index.html`

## 🔗 Важные URL после деплоя

- **Frontend**: `https://your-project.amvera.io/docs/index.html`
- **Admin**: `https://your-project.amvera.io/docs/admin.html`
- **API**: `https://your-project.amvera.io/api/applications`

## ⚙️ Переменные окружения для Amvera

Минимальный набор:
```
BOT_TOKEN=123456789:ABC...
ADMIN_PASSWORD=your_password
```

Полный набор (если используете все функции):
```
BOT_TOKEN=...
ADMIN_PASSWORD=...
MINIO_SECRET_KEY=...
DADATA_TOKEN=...
DADATA_SECRET=...
DB_HOST=db
DB_NAME=graffiti_db
DB_USER=admin
DB_PASSWORD=securepass123
```

## 🐛 Быстрые решения проблем

| Проблема | Решение |
|----------|---------|
| 502 Bad Gateway | Проверьте логи в Amvera, убедитесь что порт 5000 |
| CORS ошибки | Убедитесь что `flask-cors` в requirements.txt |
| Бот не работает | Проверьте `BOT_TOKEN` в переменных окружения |
| БД не подключается | Проверьте переменные `DB_*` |
| Mini App не открывается | Проверьте URL в настройках бота у @BotFather |

## 📝 Команды для локального тестирования

```bash
# Запуск всех сервисов локально
docker-compose up -d

# Просмотр логов
docker-compose logs -f backend

# Остановка
docker-compose down

# Пересборка после изменений
docker-compose up -d --build backend
```

## 🔄 Автообновление

После первого деплоя включите **Auto Deploy** в Amvera:
- Настройки проекта → Auto Deploy → ON
- Теперь каждый `git push` автоматически обновит приложение!

---

**Полная инструкция**: `AMVERA_DEPLOY_GUIDE.md`
