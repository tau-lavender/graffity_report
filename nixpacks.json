{
  "phases": {
    "setup": {
      "nixPkgs": ["python311"]
    },
    "install": {
      "cmds": ["pip install -r src/requirements.txt"]
    }
  },
  "start": {
    "cmd": "cd src && python -m gunicorn --bind 0.0.0.0:$PORT --workers 2 --timeout 120 main:app"
  }
}
