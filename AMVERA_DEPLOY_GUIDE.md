# 🚀 Полный гайд по деплою на Amvera

## 📋 Содержание
1. [Подготовка проекта](#подготовка-проекта)
2. [Настройка переменных окружения](#настройка-переменных-окружения)
3. [Деплой на Amvera](#деплой-на-amvera)
4. [Настройка Telegram бота](#настройка-telegram-бота)
5. [Проверка работы](#проверка-работы)
6. [Troubleshooting](#troubleshooting)

---

## 1️⃣ Подготовка проекта

### Проверьте структуру проекта:
```
graffity_report/
├── src/                    # Backend приложение
│   ├── Dockerfile
│   ├── requirements.txt
│   ├── main.py
│   └── app/
├── bot/                    # Telegram bot
│   ├── Dockerfile
│   ├── requirements.txt
│   └── bot.py
├── docs/                   # Frontend (Telegram Mini App)
│   ├── index.html
│   ├── app.js
│   └── admin.html
├── docker-compose.yml
├── .dockerignore
├── .env.example
└── amvera.yml
```

### Убедитесь, что файлы готовы:
- ✅ `.dockerignore` - исключает ненужные файлы из Docker образа
- ✅ `.env.example` - шаблон переменных окружения
- ✅ `amvera.yml` - конфигурация для Amvera
- ✅ `docker-compose.yml` - оркестрация сервисов

---

## 2️⃣ Настройка переменных окружения

### Создайте файл `.env` (НЕ коммитьте его в Git!):

```bash
# В корне проекта
cp .env.example .env
```

### Заполните `.env` реальными значениями:

```env
# Telegram Bot
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz  # Получите у @BotFather

# Database (оставьте как есть для Docker)
DB_HOST=db
DB_NAME=graffiti_db
DB_USER=admin
DB_PASSWORD=securepass123  # Измените на сильный пароль!

# MinIO (хранилище файлов)
MINIO_ENDPOINT=http://minio:9000
MINIO_ACCESS_KEY=graffiti_bot
MINIO_SECRET_KEY=your_strong_secret_key_here_min_32_chars  # Сгенерируйте случайный ключ!
MINIO_BUCKET=graffiti-reports

# DaData API (для геокодирования адресов)
DADATA_TOKEN=your_dadata_token_here
DADATA_SECRET=your_dadata_secret_here

# Admin Panel Password
ADMIN_PASSWORD=your_secure_admin_password  # Пароль для admin.html
```

### Как получить API ключи:

1. **BOT_TOKEN** - Telegram Bot:
   - Напишите [@BotFather](https://t.me/BotFather)
   - Команда: `/newbot`
   - Следуйте инструкциям
   - Скопируйте токен

2. **DADATA** API (опционально):
   - Регистрация: https://dadata.ru
   - Перейдите в раздел API
   - Скопируйте токен и секретный ключ

3. **MINIO_SECRET_KEY** - сгенерируйте случайный ключ:
   ```bash
   # Linux/Mac
   openssl rand -base64 32

   # Windows PowerShell
   -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})
   ```

---

## 3️⃣ Деплой на Amvera

### Шаг 1: Зарегистрируйтесь на Amvera
1. Перейдите на https://amvera.ru
2. Создайте аккаунт (можно через GitHub)
3. Подтвердите email

### Шаг 2: Подключите GitHub репозиторий
1. В панели Amvera нажмите **"Создать проект"**
2. Выберите **"Из GitHub репозитория"**
3. Авторизуйте Amvera доступ к вашим репозиториям
4. Выберите репозиторий `graffity_report`
5. Выберите ветку `main`

### Шаг 3: Настройте проект

#### 3.1 Основные настройки:
- **Название проекта**: `graffity-report`
- **Тип проекта**: `Docker Compose`
- **Порт**: `5000` (для backend)

#### 3.2 Переменные окружения в Amvera:
Добавьте все переменные из вашего `.env` файла:

```
BOT_TOKEN=ваш_токен
ADMIN_PASSWORD=ваш_пароль
MINIO_SECRET_KEY=ваш_секретный_ключ
DADATA_TOKEN=ваш_токен
DADATA_SECRET=ваш_секрет
```

⚠️ **ВАЖНО**: Некоторые переменные уже есть в docker-compose.yml, но критичные (токены, пароли) должны быть в переменных окружения Amvera!

#### 3.3 Настройка БД (если нужна внешняя):
Amvera предоставляет PostgreSQL addon:
1. В настройках проекта → **Addons**
2. Выберите **PostgreSQL**
3. Создайте базу данных
4. Amvera автоматически создаст переменные `DATABASE_URL`
5. Обновите переменные окружения:
   ```
   DB_HOST=<из DATABASE_URL>
   DB_NAME=<из DATABASE_URL>
   DB_USER=<из DATABASE_URL>
   DB_PASSWORD=<из DATABASE_URL>
   ```

### Шаг 4: Деплой
1. Нажмите **"Задеплоить"** или **"Deploy"**
2. Amvera автоматически:
   - Склонирует репозиторий
   - Запустит `docker-compose build`
   - Запустит `docker-compose up`gi
3. Дождитесь успешного деплоя (5-10 минут)

### Шаг 5: Получите URL приложения
После успешного деплоя Amvera даст вам URL вида:
```
https://your-project.amvera.io
```

---

## 4️⃣ Настройка Telegram бота

### Обновите URL в коде
Замените `https://thefid.pythonanywhere.com` на ваш Amvera URL во всех файлах:

**В `docs/app.js`:**
```javascript
// Было:
fetch('https://thefid.pythonanywhere.com/api/apply', {

// Стало:
fetch('https://your-project.amvera.io/api/apply', {
```

**В `docs/admin.html`:**
```javascript
// Было:
let res = await fetch('https://thefid.pythonanywhere.com/api/applications');

// Стало:
let res = await fetch('https://your-project.amvera.io/api/applications');
```

### Настройте Telegram Mini App
1. Откройте [@BotFather](https://t.me/BotFather)
2. Выберите вашего бота
3. Команда: `/setmenubutton`
4. Выберите бота
5. Введите текст кнопки: **"Открыть приложение"**
6. Введите URL: `https://your-project.amvera.io/docs/index.html`

Или используйте команду `/newapp`:
- Введите название приложения
- Загрузите иконку (512x512 px)
- Введите описание
- Укажите URL: `https://your-project.amvera.io/docs/index.html`

---

## 5️⃣ Проверка работы

### 1. Проверьте Backend API:
```bash
curl https://your-project.amvera.io/api/applications
```

Должен вернуть JSON: `[]` или список заявок

### 2. Проверьте Frontend:
Откройте в браузере:
```
https://your-project.amvera.io/docs/index.html
```

### 3. Проверьте Telegram Mini App:
1. Откройте бота в Telegram
2. Нажмите кнопку меню
3. Должно открыться ваше приложение

### 4. Проверьте Admin панель:
```
https://your-project.amvera.io/docs/admin.html
```
Введите пароль из `ADMIN_PASSWORD`

---

## 6️⃣ Troubleshooting

### ❌ Проблема: Приложение не запускается

**Решение:**
1. Проверьте логи в Amvera:
   - Перейдите в **Logs** в панели проекта
   - Найдите ошибки в логах контейнеров

2. Проверьте переменные окружения:
   - Все ли критичные переменные заданы?
   - Нет ли опечаток?

### ❌ Проблема: 502 Bad Gateway

**Решение:**
1. Backend не запустился. Проверьте:
   ```bash
   # В логах должно быть:
   # [INFO] Listening at: http://0.0.0.0:5000
   ```

2. Проверьте `src/Dockerfile` - правильно ли указан CMD:
   ```dockerfile
   CMD ["gunicorn", "--bind", "0.0.0.0:5000", "--workers", "2", "main:app"]
   ```

### ❌ Проблема: База данных не подключается

**Решение:**
1. Если используете встроенный PostgreSQL в docker-compose:
   - Убедитесь, что сервис `db` запущен
   - Проверьте логи: `docker-compose logs db`

2. Если используете Amvera PostgreSQL addon:
   - Проверьте правильность переменных DB_*
   - Используйте внутренний URL БД от Amvera

### ❌ Проблема: CORS ошибки

**Решение:**
Убедитесь, что в `src/app/__init__.py` включен CORS:
```python
from flask_cors import CORS
app = Flask(__name__)
CORS(app)  # Разрешаем CORS для всех доменов
```

### ❌ Проблема: Telegram Mini App не открывается

**Решение:**
1. Проверьте URL в настройках бота у @BotFather
2. URL должен начинаться с `https://` (не http://)
3. Файл должен быть доступен публично
4. Убедитесь, что подключен Telegram SDK:
   ```html
   <script src="https://telegram.org/js/telegram-web-app.js"></script>
   ```

### ❌ Проблема: MinIO не работает

**Решение:**
1. MinIO нужен только для хранения фотографий
2. Если не используете фото - можете удалить сервис `minio` из docker-compose.yml
3. Если нужен - проверьте доступность порта 9000

---

## 📝 Полезные команды

### Локальная разработка:
```bash
# Запуск всех сервисов
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Пересборка после изменений
docker-compose up -d --build
```

### Git команды для деплоя:
```bash
# Добавить все изменения
git add .

# Закоммитить
git commit -m "Update for Amvera deploy"

# Запушить на GitHub
git push origin main

# Amvera автоматически задеплоит изменения!
```

---

## 🎉 Готово!

Ваше приложение теперь работает на Amvera!

**Ссылки:**
- 🌐 **Frontend**: `https://your-project.amvera.io/docs/index.html`
- ⚙️ **Admin**: `https://your-project.amvera.io/docs/admin.html`
- 🤖 **Bot**: Ваш бот в Telegram

---

## 🔄 Автоматический деплой

Amvera поддерживает **автодеплой** при каждом push в GitHub:

1. В настройках проекта включите **Auto Deploy**
2. Выберите ветку `main`
3. Теперь каждый `git push` автоматически обновит приложение!

---

## 💡 Рекомендации

1. **Безопасность:**
   - Никогда не коммитьте `.env` в Git
   - Используйте сильные пароли
   - Регулярно меняйте токены

2. **Мониторинг:**
   - Регулярно проверяйте логи в Amvera
   - Настройте уведомления о падении сервиса

3. **Бэкапы:**
   - Делайте резервные копии базы данных
   - Экспортируйте важные данные

4. **Масштабирование:**
   - При необходимости увеличьте количество workers в gunicorn
   - Используйте Redis для кэширования (можно добавить в docker-compose)

---

**Удачи с вашим проектом! 🚀**

Если возникнут вопросы - обращайтесь в поддержку Amvera или проверьте документацию: https://amvera.ru/docs
